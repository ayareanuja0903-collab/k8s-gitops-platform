apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: devops-project
  namespace: argocd

spec:
  description: DevOps GitOps Project

  sourceRepos:
    - '*'

  destinations:
    - namespace: '*'
      server: '*'